const React = require('react');
import { PAGES } from '../constants';

module.exports = class PageTeamcomp extends React.Component {
  render() {
    return (
      <section>
        temacomp
      </section>
    );
  }
};